$('.rating').raty( { path: '/assets', scoreName: 'comment[rating]' });
$('.rated').raty({ path: '/assets',
  readOnly: true,
  score: function() {
    return $(this).attr('data-score');
  }
});

$('.new_comment').html("Thank you for your review on the product!");

$('#<%=product.id%>reviews').prepend("<%= j render @comment %>");

$('#commentModal<%=product.id%>').modal('hide');